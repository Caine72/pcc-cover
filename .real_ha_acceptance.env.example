# Copy to .real_ha_acceptance.env and set real values.
# This file is for local use and should stay untracked.

HA_URL=https://localhost:8123
HA_TOKEN=replace-with-long-lived-access-token
PCC_ENTITY_ID=cover.replace_with_pcc_entity

# Optional container-local overrides:
# HA_CONFIG_DIR=/workspaces/home-assistant-core-dev/config
# HA_ACCEPTANCE_OUTPUT_DIR=/tmp
